<h1>List of artists</h1>

<%= link_to "Create a New Artists", new_admin_artist_path %>

<ul>
  <% @artists.each do |artist| %>
    <li>
      <%= link_to artist.name, admin_artist_path(artist) %>
    </li>
  <% end %>
</ul>
