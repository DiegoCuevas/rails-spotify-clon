<h1>List of songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li>
      <%= link_to song.title, admin_song_path(song) %>
    </li>
  <% end %>
</ul>


<%= link_to "Create a New Song", new_admin_song_path %>